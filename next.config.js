/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "greensoftware-seven.vercel.app"],
  },
};

module.exports = nextConfig;
